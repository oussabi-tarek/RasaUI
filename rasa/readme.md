# open rasa folder

open 2 commands prompt
in the first run:
rasa run actions # for starting server
the second run:
rasa run --cors "\*" --enable-api # for enabling api

# open react floder

npm start
